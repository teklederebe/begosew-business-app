name: begosew_business_app
description: Cement & Rebar business app for Ethiopian shops
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
syncfusion_flutter_pdfviewer: ^23.1.36
  flutter:
    sdk: flutter
  firebase_core: ^2.0.0
  firebase_auth: ^4.0.0
  cloud_firestore: ^4.0.0
  provider: ^6.0.0
  intl: ^0.18.0
  charts_flutter: ^0.12.0
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
  generate: true
